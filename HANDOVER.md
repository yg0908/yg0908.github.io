개발셋팅

1. 작업폴더에 shift + 우클릭해서 여기서 powershell 열기를 누릅니다.

2. 터미널에 npm create vite@latest 입력

3. 프로젝트이름 입력

4. 프레임워크 선택 React

5. javascript 선택

화면대로 입력
-----------------------------------------------------------------------
-오류시

윈도우인데 "허가되지 않은 스크립트 입니다 어쩌구~" 에러

윈도우 하단 검색메뉴에서 Powershell 검색 - 우클릭 - 관리자 권한으로 실행한 뒤

Set-ExecutionPolicy RemoteSigned 입력합시다.

그 다음에 뭐 동의하라고 하면 y 누르고 엔터칩시다.

그 다음에 터미널 껐다켜면 npm 명령어들이 잘될 수 있습니다.
-----------------------------------------------------------------------

App.jsx .js 는 상관없음

터미널에서 npm run dev

리액트는 싱글 페이지 어플리케이션
App.js -> main.js -> index.html 로 보여짐

package.json: 설치한 라이브러리 기록용


state 는 자주 변경될것 같은 데이터들에 쓰는게 좋음


onClick={} 은 버튼을 눌렀을때 이벤트

안에 함수를 넣어야함 function 함수(){} 이런식 하지만 () =>{} 이런식으로도 축약가능

state변경하는법
state변경함수 (새로운state)

let copy = [...글제목]; 에서 ...는 괄호를 벗겨주세요 라는것 arr 안에 있는게 아닌 새로운 화살표 를 만드는것

만약 arr 안에 데이터를 수정하려면 새로운 화살표에 저장하고 ...를 써야함

--------------------------------------------------------------------------------------------
ex) let [글제목, 글제목변경] = useState(['남자 코트 추천', '강남 우동맛집', '파이썬 독학']);

<button onClick={()=>{
    let copy = [...글제목];
    copy[0] = '여자코트 추천';
    글제목변경(copy);
    }}>글수정</button>
--------------------------------------------------------------------------------------------

## 작성일: 2026-04-21

동적인 UI 만드는 step
1. html css로 미리 디자인완성
2. UI의 현재 상태를 state로 저장
3. state에 따라 UI가 어떻게 보일지 작성

html 중간에 조건문 쓰려면 상항연산자
{
    조건 ? 참일때 실행할 코드 : 거짓일때 실행할 코드
}







